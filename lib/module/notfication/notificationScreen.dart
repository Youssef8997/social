import 'package:flutter/material.dart';

class notfication extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("notfication screeen"),
    );
  }
}
